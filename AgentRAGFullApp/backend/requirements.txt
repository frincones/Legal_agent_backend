# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pyyaml>=6.0

# LLM & Embeddings (OpenAI Realtime requires >=1.50)
openai>=1.50.0

# Database
asyncpg>=0.29.0

# LexAI · Auth + Voice
python-jose[cryptography]>=3.3.0       # Supabase JWT verification (HS256)
websockets>=12.0                        # OpenAI Realtime WebSocket relay

# Document Processing (Level 3 ingestion)
docling>=2.0.0
transformers>=4.36.0

# Re-ranking
sentence-transformers>=2.2.0

# Lightweight readers (used as fast path / Docling fallback)
pypdf>=6.0.0
python-docx>=1.1.0

# Legal Sources - HTTP client and HTML parsing
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Utilities
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Note: psutil is intentionally NOT installed.
# It is a transitive dependency of `accelerate` (HuggingFace) but is only used
# at runtime for memory monitoring during model loading. Since we use OpenAI
# API embeddings and pypdf for PDFs (not local accelerate-managed models),
# removing psutil has no impact on functionality.
